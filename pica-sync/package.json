{
  "name": "pica-sync",
  "version": "1.0.0",
  "description": "Sincroniza el Notion de Pica con el skill de Antigravity IDE",
  "type": "module",
  "scripts": {
    "sync": "node sync.js --once",
    "watch": "node sync.js --watch",
    "start": "node sync.js --watch"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "notion-to-md": "^3.1.1",
    "chokidar": "^3.6.0"
  }
}
